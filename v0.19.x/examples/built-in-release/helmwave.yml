repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
- name: a
  chart:
    name: bitnami/nginx
  namespace: test
  values:
  - _.yml

- name: b
  chart:
    name: bitnami/nginx
  namespace: test
  values:
    - _.yml
