project: "Example: private env"
version: "⟨⟨ ver ⟩⟩"

repositories:
  - name: jetstack
    url: https://charts.jetstack.io

releases:
  - name: cert-manager
    chart:
      name: jetstack/cert-manager
      version: v1.12.1
    offline_kube_version: 1.22.2
    namespace: cert-manager
