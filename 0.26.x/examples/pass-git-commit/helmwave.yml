version: 0.25.0

repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: my-app
    chart: bitnami/nginx
    values:
      - values.yml
    namespace: my-namespace
    create_namespace: true
