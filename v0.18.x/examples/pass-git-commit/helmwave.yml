version: 0.18.0

repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: my-app
    chart:
      name: bitnami/nginx
    values:
      - values.yml
    namespace: my-namespace
    createnamespace: true
