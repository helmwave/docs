version: 0.20.1

repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: my-app
    chart:
      name: bitnami/nginx
    values:
      - values.yml
    namespace: my-namespace
    create_namespace: true
