version: 0.27.2

repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: my-app
    chart: bitnami/nginx
    values:
      - values.yml
    namespace: my-namespace
    create_namespace: true
